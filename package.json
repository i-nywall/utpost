{
  "name": "utpost",
  "private": true,
  "version": "1.4.2",
  "workspaces": ["api", "web", "client"],
  "scripts": {
    "dev": "concurrently -n api,web,client -c blue,green,red \"npm run dev --workspace=api\" \"npm run dev --workspace=web\" \"npm run dev --workspace=client\"",
    "dev:api": "npm run dev --workspace=api",
    "dev:web": "npm run dev --workspace=web",
    "dev:cli": "npm run dev --workspace=api",
    "seed": "npm run seed --workspace=api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
